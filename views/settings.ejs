<div class="row">
  <div class="span8">
    <div class='content'>
      <form method="post">
        <fieldset>
          <legend>Setting</legend>
          <textarea id="content" name="content"><%= JSON.stringify(config,null,4) %></textarea>
        </fieldset>
        <button class="btn btn-primary" type="submit">Save</button>
      </form>
    </div>
  </div>
</div>
